# backend\app\tests\README.md

This directory will contain pytest files for unit and integration testing. Structure should mirror the `app/` directory (e.g., `tests/api/`, `tests/services/`).

**Status:** Not yet implemented. Test suite will be added before payment integration.
